// swap this to a sql database eventually
public class BankrollManager {
    private static final String DATA_FILE = "bankroll_data.txt";

    public static void saveBankroll(User user) {
        // save users bankroll to file
    }

    public static Balance loadBankroll(String username) {
        // load users bankroll from the file
        // return the loaded balance

        return null;
    }
}
